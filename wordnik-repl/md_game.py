"""A simple game to refine api.saveList and repl formatting."""


# import random

def master_dict(dictionary):
    length = len(dictionary)
    print('There are {} entries in this dictionary'.format(length))
